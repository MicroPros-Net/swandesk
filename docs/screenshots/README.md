# Screenshots

Put product screenshots here and reference them from the main `README.md`.

Suggested set (names the README already points at):

- `dashboard.png` — the main dashboard with charts
- `inbox.png` — the Outlook-style ticket inbox
- `inventory.png` — the asset inventory / network map

Tip: the images already published in the `/swandesk/` marketing gallery on
micropros.net are a good source — export them at ~1400px wide, PNG.
